# Scripts shell precisam de LF (senao quebram no CI Linux)
*.sh text eol=lf
gradlew text eol=lf
# SQL (migrations, seed e testes) tambem: o psql gset/ir quebra com CRLF
*.sql text eol=lf
# Binarios
*.jar binary
*.keystore binary
*.png binary
